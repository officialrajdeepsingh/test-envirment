
name: Environment Variables List
on: 
    push:
         branches:
           - main
jobs:

  github-actions-environment-variables-ubuntu:
    runs-on: ubuntu-latest
    steps:
      - name: List of the GitHub Actions 
        run: ${{ secrets.NPM_AUTH_TOKEN.NPM_AUTH_TOKEN2 }}
      - name: GitHub Actions 
        run: env